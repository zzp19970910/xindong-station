# 前后端分离部署时，把下面这行改成都您真实后端的地址（含/api/v1）
# 本机联调示例（后端跑8080，前端跑5173或任何端口）：
VITE_API_BASE=http://localhost:8080/api/v1
# 生产服务器示例（后端已部署到api.example.com）：
# VITE_API_BASE=https://api.example.com/api/v1
#
# 如果是合包模式（单jar承载前后端）：请删除/注释本变量，默认同源路径 /api/v1 即可